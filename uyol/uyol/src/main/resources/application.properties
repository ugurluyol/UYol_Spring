# =====================
# APP
# =====================
spring.application.name=UYol
server.port=10000
server.servlet.context-path=/uyol

# =====================
# DATABASE (DEV STUB)
# =====================
spring.datasource.url=jdbc:postgresql://localhost:5432/uyol
spring.datasource.username=uyol
spring.datasource.password=uyol
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.open-in-view=false

# ?? IMPORTANT: disable Flyway for now
spring.flyway.enabled=false

# =====================
# SECURITY (DISABLED FOR DEV)
# =====================
# Disable Keycloak / JWT auto config
spring.security.oauth2.resourceserver.jwt.issuer-uri=

# =====================
# MAIL (DEV / MAILPIT)
# =====================
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=dev@example.com
spring.mail.password=devpassword

# =====================
# TWILIO (DEV / DUMMY)
# =====================
phone.dev.account.sid=AC00000000000000000000000000000000
phone.dev.auth.token=dev_dummy_token_123456
